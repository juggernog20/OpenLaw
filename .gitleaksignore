# SPDX-License-Identifier: AGPL-3.0-only
#
# False-positive fingerprints for the gitleaks history scan
# (.github/workflows/security.yml). One fingerprint per line:
# commit:file:rule:line, copied from the finding in the job log.
# Fingerprints pin a commit, so they stay valid for the weekly
# full-history scan; the line itself also carries a gitleaks:allow
# marker so future commits touching it are not re-flagged.

# Test fixture: the change-password round-trip's new password, an
# xkcd-style phrase like the harness admin password. Not a secret.
fb3c9439823e9d0e824e596d393fb7e96d1e130b:apps/api/src/modules/auth/profile.test.ts:generic-api-key:171

# Pencil's own per-file `fileToken`, in the M-stage design mock. Every
# .pen file in designs/ carries one — it identifies the file to the
# editor and grants nothing outside it. The line cannot carry a
# gitleaks:allow marker, because a .pen file is encrypted and is only
# ever written by the editor.
1978bbae1658790dd0ba67effc60a2782c083be4:designs/stage-component-design.pen:generic-api-key:16851

# Public source commit for the documentation fixture API build, not an API key.
# The metadata field is now backendSourceCommit to avoid the misleading key name.
6d0f5bd4d412da406b6e39ea8ec8be5802250c85:docs/documentation/help-readiness.json:generic-api-key:7

# Public guide-content SHA-256 values, matched because the article ID contains
# "access". The current records use separate articleId and contentSha256 fields.
294cc3175d1e5c747b3a1ee9790ae622b8caab6b:docs/documentation/batches/DOC-010/author-recovery.json:generic-api-key:6
294cc3175d1e5c747b3a1ee9790ae622b8caab6b:docs/documentation/batches/DOC-010/author-discovery.json:generic-api-key:7
