# SPDX-License-Identifier: AGPL-3.0-only
#
# Gitleaks configuration for the Secret scan (history) CI job
# (.github/workflows/security.yml). It extends the default rules; the
# allowlist below names known non-secrets one by one, never whole files
# or directories. The scan covers commit history, so a flagged fixture
# is allowlisted here rather than reworded — rewording only changes the
# tip, and the finding stays in the commits that already carry it.

[extend]
useDefault = true

[allowlist]
description = "Fixture values that look like credentials and are not"
# "not-a-real-key" in base64 — the Azure account-key fixture in
# apps/api/src/lib/storage/config.test.ts.
#
# The two RSA-key-shaped fixtures in
# apps/api/src/modules/signing-connector/signing-connector.test.ts. They
# carry a PEM header so the suite proves what a pasted key does, and the
# body says in words that it is neither a key nor real. The DocuSign
# driver's own suite generates a genuine key pair at run time and stores
# none.
#
# The same shape again in
# apps/api/src/modules/onboarding/onboarding.test.ts, where the
# wizard's e-signature step reads whether a connector row exists and
# a connector row has to hold something key-shaped to be one.
#
# A bare PEM header typed into a key box by a web route test: the
# default private-key rule reads from one header to the next and calls
# everything between them a key, so two such lines in one file are read
# as one long secret. The header is followed here by the close of the
# `user.type()` call that typed it, which is why the pattern ends in
# `");` — a real key carries base64 there, never a string terminator.
#
# DOC-029 documentation walkthrough logs under
# docs/documentation/batches/DOC-029/. Five sha256 digests of guide
# Markdown, recorded as "<article-id>": "<digest>" so each run names the
# exact text it followed. The generic-api-key rule reads the article ID
# as a key name. Then three fictional stand-in values: the AI key the
# credential-free checks type into a connector form, and the integration
# key of the local DocuSign stand-in server.
#
# DOC-030 documentation walkthrough logs under
# docs/documentation/batches/DOC-030/. Four sha256 digests of guide
# Markdown, recorded the same way as in DOC-029. One more digest is the
# suffix of a Field slug (`key_date:<digest>`) that the Analysis run
# logged. Two UUIDv7 record IDs of API key requests in the conversations
# fixtures. Then the public development account key of Azurite, the
# Azure Storage emulator, which Microsoft publishes in its own docs.
# Last, the fictional invalid AI key and the DocuSign stand-in
# integration key. The real OpenRouter key of the live Analysis check
# went into no file.
regexes = [
  '''^0a4b028bcaadc3fc71c1a3b820ad9e6fd5af217c3db2be999695c3beb4136c8f$''',
  '''^24154ab5101e2dd5148e99935ad7ad3e3a4d0562f844b4db89a6a0ffaa9da1a7$''',
  '''^cdee48674cb2f6088c8a3c44160a80d121883585b5f045081ee7e3cc4a4a3733$''',
  '''^d81e8cf14f7a9bbbb520d3823cac40df554d8353510279f6eaa01e89bd2ae810$''',
  '''^76000a6004dfeda8db41a88ad608146a809d90f1f67a6ffe31ac475d0e8ce8db$''',
  '''^01a0d83d-b240-7171-b748-a5c8c3804521$''',
  '''^01a0d83e-5fe8-7554-9b45-1811869b90d4$''',
  '''^Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==$''',
  '''^doc030-fictional-invalid-key-0000$''',
  '''^doc030-standin-integration-key$''',
  '''^72be10f398e150f7d280aa0ba0d00d76033ee6d00c95314d5d57bd12757d0aa1$''',
  '''^b5e625752297ce38c24e0aa30609d615ef7db10ff366a45e827c869c02c3b208$''',
  '''^4785f9d115bbf7c8038eef77875a9da731c695930445a7b8e91a1b9ba84f2df8$''',
  '''^e6b84c136d32049a04b1827512c6afcea0c1771d79d23a4829517bdec8de8042$''',
  '''^f6d077cc3b817a0c097844fb00d800525777503bab0ba0bff52b222f14b1ea2f$''',
  '''^fictional-doc029-key$''',
  '''^fictional-doc029r2-key$''',
  '''^doc029-standin-integration-key$''',
  '''bm90LWEtcmVhbC1rZXk=''',
  '''openlawfixturekeyneverused''',
  '''openlawonboardingfixturekey''',
  '''-----BEGIN RSA PRIVATE KEY-----"\);''',
]
