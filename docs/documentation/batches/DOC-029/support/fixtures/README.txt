Fictional fixtures for the DOC-029 support independent walkthrough (round 1).
doc029-support-damaged.docx is a ZIP container with random member bytes, so the document engine cannot convert it.
The 105 MiB oversized file and the 300-character filename are built in memory by walkthrough-r1.mjs and never stored.
