{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // DOM alongside node: page.evaluate / locator.evaluate callbacks
    // are typed as browser code even though the suite runs in Node.
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["node"]
  },
  "include": ["tests", "playwright.config.ts"]
}
